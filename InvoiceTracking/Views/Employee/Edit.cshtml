@model EntityLayer.Entities.Employee

@{
    ViewData["Title"] = "Çalışan Güncelle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h2 class="text-center">Çalışan Güncelle</h2>

    <form asp-action="Edit" method="post" class="shadow p-4 bg-light rounded">
        <input type="hidden" asp-for="EmployeeId" />

        <div class="mb-3">
            <label class="form-label">Ad</label>
            <input asp-for="EmployeeName" class="form-control" required />
        </div>

        <div class="mb-3">
            <label class="form-label">Soyad</label>
            <input asp-for="EmployeeSurname" class="form-control" required />
        </div>

        <button type="submit" class="btn btn-primary">Güncelle</button>
        <a asp-action="Index" class="btn btn-secondary">İptal</a>
    </form>
</div>
