@model IEnumerable<EntityLayer.Entities.Payment>

@{
    ViewData["Title"] = "Ödeme Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="mb-4">@ViewData["Title"]</h2>

<a asp-action="Create" class="btn btn-primary mb-3">
    <i class="fas fa-plus"></i> Yeni Ödeme Ekle
</a>

<div class="table-responsive">
    <table id="paymentTable" class="table table-striped table-hover align-middle">
        <thead class="table-dark">
            <tr>
                <th>Müşteri</th>
                <th>Tarih</th>
                <th>Tutar</th>
                <th>Para Birimi</th>
                <th class="text-center">İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var payment in Model)
            {
                <tr>
                    <td>@payment.Client?.CompanyName</td>
                    <td>@payment.Date.ToString("dd/MM/yyyy")</td>
                    <td>@payment.Amount.ToString("N2")</td>
                    <td>@payment.Currency</td>
                    <td class="text-center">
                        <a asp-action="Details" asp-route-id="@payment.PaymentId" class="btn btn-info btn-sm" title="Detay">
                            <i class="fas fa-eye"></i>
                        </a>
                        <a asp-action="Edit" asp-route-id="@payment.PaymentId" class="btn btn-warning btn-sm" title="Düzenle">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@payment.PaymentId" class="btn btn-danger btn-sm" title="Sil">
                            <i class="fas fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#paymentTable').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.11.5/i18n/Turkish.json"
                },
                "paging": true,
                "ordering": true,
                "info": true
            });
        });
    </script>
}
